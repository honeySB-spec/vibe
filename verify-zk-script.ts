import { ZKAuth } from './lib/zk-auth'; async function test() { console.log('1. Generating Identity...'); const id = ZKAuth.generateIdentity(); console.log('Secret:', id.secret); console.log('Public:', id.public); console.log('2. Creating Proof...'); const context = 'test-context'; const proof = ZKAuth.createProof(id.secret, context); console.log('Proof:', proof); console.log('3. Verifying locally...'); const isValid = ZKAuth.verifyProof(id.public, proof, context); console.log('Valid?', isValid); } test();
